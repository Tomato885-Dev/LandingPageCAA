{
  "name": "landing-caa",
  "version": "1.0.0",
  "private": true,
  "description": "Landing page de campana CAA. El sitio funciona sin instalar nada; esto es solo para generar la version optimizada que se publica.",
  "scripts": {
    "build": "node build/construir.mjs"
  },
  "devDependencies": {
    "@babel/core": "^7.26.0",
    "@babel/preset-react": "^7.26.3",
    "esbuild": "^0.24.2",
    "tailwindcss": "^3.4.17"
  }
}
